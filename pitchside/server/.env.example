PORT=5000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/pitchside
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:3000

# Optional. Without it, the server falls back to deterministic mock data.
# Free key: https://www.football-data.org/client/register
FOOTBALL_DATA_KEY=
